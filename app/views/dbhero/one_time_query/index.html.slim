.section.bottom-bord.grey.lighten-4
  .container
    .row
      .col.s12.center-align
        h5.clip-title One Time Query

.container
  == render 'one_time_query_form'

- if @dataclip_params.present? && @dataclip&.query_valid?
  == render 'clip_table'
